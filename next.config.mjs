/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    // No ESLint config is shipped in this scaffold; keep `next build` green.
    ignoreDuringBuilds: true,
  },
};

export default nextConfig;
